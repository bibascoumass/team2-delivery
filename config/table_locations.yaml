safety_thresholds:
  warning_dist: 0.6  # transition to AVOID_DYNAMIC
  critical_dist: 0.2  # transition to ESTOP


delivery_settings:
  dock_wait_secs: 5.0 # seconds to wait for delivery confirmation
  scan_max_angle_deg: 360  # max rotation during SCAN_FOR_QR
  scan_angular_vel_degs: 15.0 # rotation speed during scan (deg/s)
